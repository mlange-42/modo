input: test.json
output: out/
format: plain
doctest: doctest/
exports: true
short-links: true
case-insensitive: true

pre-build:
  - echo Starting build.
  - |
    cd out
    ls

post-build:
  - echo Build finished

pre-test:
  - echo Starting tests.

post-test:
  - echo Tests finished
